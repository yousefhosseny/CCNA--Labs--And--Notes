بالطبع! إليك **شرح مبسط جدًا** لقسم **"Router and Switch File Maintenance"** — بطريقة سهلة، عملية، وواضحة، كأنك مع مهندس خبير بيشرحلك على كوباية قهوة ☕:

---

### 🗂 أولًا: إيه المقصود بـ **File Maintenance**؟  
هي ببساطة:  
> **"إدارة الملفات"** في أجهزة سايسكو:  
> - شغّل/احفظ إعدادات  
> - احفظ نسخة احتياطية  
> - رجّع إعدادات قديمة  
> - استرد جهاز نسيت عليه الباسورد 😅

---

## 🔹 1. أنظمة الملفات في الراوتر والسويتش (File Systems)

### 📁 أين تُخزّن الملفات؟
| المكان | الوظيفة | مثال |
|-------|---------|------|
| **`flash:`** | تخزين ملفات النظام (IOS) والنسخ الاحتياطية | `c3745-ipbase-mz.124-15.T14.bin` |
| **`nvram:`** | تخزين ملف الإعدادات **الابتدائية** (startup-config) | `startup-config` |
| **`usbflash0:`** | (في بعض الراوترات) → فلاشة USB متصلة بالجهاز | لنسخ احتياطي خارجي |
| **`tftp:`** | سيرفر خارجي على الشبكة (TFTP Server) | لحفظ النسخ من بعيد |

> ✅ افتراضيًا: الجهاز يفتح على `flash:` (فيها ملف النظام والملفات المهمة).

---

### 📋 كيف نشوف الملفات؟

| الأمر | الوظيفة |
|-------|---------|
| `show file systems` | يظهر كل أنظمة الملفات المتاحة (مثل `flash:`, `nvram:`, `usbflash0:`) |
| `dir` | يظهر محتويات المجلد الحالي (افتراضيًا: `flash:`) |
| `dir nvram:` | يظهر محتويات الـ NVRAM (يعني: الملفات المخزنة هناك) |
| `cd` | تغيير المجلد (مثل `cd nvram:`) |
| `pwd` | يظهر المجلد الحالي (مثل: `nvram:`) |

> 💡 في `flash:` لازم تلاقي:
> - ملف الـ **IOS** (مثل: `c2900-universalk9-mz.SPA.155-3.M.bin`)
> - وربما نسخ احتياطية (`R1-backup.cfg`)

> وفي `nvram:` لازم تلاقي:
> - ملف اسمه **`startup-config`** ← ده اللي بيتحمل أول ما الجهاز يشتغل.

---

## 🔁 2. طرق حفظ وتحميل الإعدادات (Back Up & Restore)

### 📤 أولاً: **حفظ إعدادات لملف نصي (Text File)**  
(باستخدام برنامج زي **Tera Term** أو **PuTTY**)

#### ✅ الحفظ (Backup):
1. في Tera Term: **File → Log**  
2. اختار مكان لحفظ الملف (مثل: `C:\Backups\R1-config.txt`)  
3. في الراوتر:  
   ```bash
   Router# show running-config
   ```
   → كل اللي يظهر يُكتب تلقائيًا في الملف.  
4. خلّص → **File → Close Log**

#### ✅ الرجوع (Restore):
1. عدل الملف (احذف: `--More--`، والرسائل غير المهمة، وتأكد إن الباسوردات مش مشفرة إذا حابب تعدلها).  
2. في Tera Term: **File → Send file**  
3. اختار الملف → البرنامج هيبدأ يلصق السطور كلها في الراوتر تلقائيًا! 🚀  
→ الإعدادات الجديدة بتصبح **running-config** فورًا.

> ⚠️ نصيحة: زود في أول الملف:
> ```
> enable
> configure terminal
> ```
> علشان يدخل وضع التعديل تلقائيًا.

---

### 🌐 ثانيًا: **استخدام سيرفر TFTP** (الأفضل للمحترفين)

#### ✅ الحفظ إلى TFTP:
```bash
R1# copy running-config tftp
Address or name of remote host []? 192.168.1.100   ← IP السيرفر
Destination filename [R1-confg]? R1-backup-2026.cfg
!!
Writing R1-backup-2026.cfg !! [OK]
```

#### ✅ الرجوع من TFTP:
```bash
R1# copy tftp running-config
Address or name of remote host []? 192.168.1.100
Source filename []? R1-backup-2026.cfg
!!
[OK]
```

> ✅ مميزات TFTP:
> - سهل الأتمتة  
> - ينفع لـ 100 جهاز  
> - ينفع تتحكم فيه بـ scripts

> ❌ عيوبه:
> - لا تشفير (UDP)  
> - لا مصادقة  
> → يُستخدم داخليًا فقط (في شبكة موثوقة)

---

### 💾 ثالثًا: **استخدام فلاشة USB** (مفيد في الميدان)

#### ✅ خطوات الحفظ:
1. شوف إذا الفلاشة ظاهرة:
   ```bash
   R1# show file systems
   ```
   ← تلاقي `usbflash0:`  
2. احفظ:
   ```bash
   R1# copy running-config usbflash0:
   Destination filename [running-config]? R1-usb-backup.cfg
   ```
3. شوف الملف:
   ```bash
   R1# dir usbflash0:
   R1# more usbflash0:/R1-usb-backup.cfg
   ```

#### ✅ خطوات الرجوع:
```bash
R1# copy usbflash0:/R1-usb-backup.cfg running-config
```

> ✅ ممتاز لو:
> - مش معاك شبكة  
> - عايز تنقل نسخة بين أجهزة  
> - تعمل Backup في موقع بعيد (بدون إنترنت)

---

## 🔐 3. استرداد الباسورد (Password Recovery)

> حصل ونسيت الباسورد؟ لا تقلق — في طريقة رسمية من سايسكو لاسترجاعه.

### ⚙️ الفكرة العامة (نفسها في كل الأجهزة):

| الخطوة | ما يحصل |
|--------|---------|
| **1️⃣ دخول وضع ROMMON** | توقف التشغيل → اضغط **Ctrl+Break** في أثناء إعادة التشغيل (في Tera Term/Console) → يظهر: `rommon 1 >` |
| **2️⃣ غير register** | تكتب:  
`rommon 1 > confreg 0x2142`  
→ ده بيقول للجهاز: **"ما تحملش startup-config أول ما تشتغل"** |
| **3️⃣ أعد التشغيل** | `rommon 2 > reset` |
| **4️⃣ شغّل الإعدادات القديمة يدويًا** | بعد ما يشتغل:  
`Router# copy startup-config running-config`  
← دي خطوة مهمّة علشان تجيب الإعدادات (ما عدا الباسوردات المشفرة) |
| **5️⃣ غير الباسورد** |  
```bash
Router# configure terminal  
Router(config)# enable secret newpassword  
Router(config)# username admin secret newpass  ← لو فيه دخول بـ username
``` 
| **6️⃣ رجّع الإعداد الطبيعي** |  
```bash
Router(config)# config-register 0x2102  ← علشان يرجع يحمل startup-config طبيعي  
Router# copy running-config startup-config  
Router# reload
```

> ✅ ملاحظات مهمة:
> - `0x2142` = تجاهل startup-config  
> - `0x2102` = السلوك الطبيعي (الافتراضي)  
> - **مش هتقدر تقرأ الباسورد القديم** — بس تقدر تغيّره ✅

---

## 🆚 مقارنة سريعة بين طرق النسخ الاحتياطي:

| الطريقة | السرعة | الأمان | سهولة | الاستخدام |
|--------|--------|--------|-------|-----------|
| **Text File (Tera Term)** | بطيئة نسبيًا | منخفض (ملف عادي) | سهلة جدًا | مختبرات، أجهزة قليلة |
| **TFTP** | سريعة | منخفض (UDP) | متوسطة | شبكات متوسطة/كبيرة |
| **USB Flash** | سريعة جدًا | متوسط (فلاشة في يدك) | سهلة | مواقع ميدانية، بدون شبكة |
| **SCP / HTTPS** (مش مذكور هنا، لكن موجود في الإصدارات الحديثة) | سريعة | عالي (تشفير) | معقدة شوية | شبكات آمنة واحترافية |

---

### ✅ خلاصة في 5 نقاط (عايز تحفظ؟ احفظ دول):

1. `flash:` = مكان النظام والملفات  
   `nvram:` = مكان `startup-config`  
2. `dir` و `cd` و `pwd` = أوامر التنقل بين المجلدات  
3. 3 طرق رئيسية للـ Backup:  
   - **Tera Term** (نص عادي)  
   - **TFTP** (شبكة)  
   - **USB** (فلاشة)  
4. النسخ الاحتياطي **ضروري** — خاصة قبل أي تغيير كبير.  
5. نسيت الباسورد؟  
   → `ROMMON` → `confreg 0x2142` → `reset` → غيّر الباسورد → `config-register 0x2102` → `reload`

---

### 🛠 نصائح عملية للمختبرات (Packet Tracer / Lab):

| المهمة | الطريقة الموصى بها |
|--------|------------------|
| نسخ احتياطي سريع | `copy run tftp` أو `copy run usbflash0:` |
| رجوع إعدادات | `copy tftp run` أو `copy usbflash0:/file run` |
| تدريب على استرداد باسورد | جرب الـ ROMMON في Packet Tracer (في وضع Physical Mode) |

---

إذا حابب، أقدر أرسل لك:
- 📋 ملف PDF جاهز: "دليل سريع لأوامر File Maintenance"
- 📄 سيناريو كامل: خطوة بخطوة — استرداد باسورد في راوتر 1941
- 🎯 قائمة بأدوات مجانية: TFTP Server / Terminal Emulator / USB Emulation

بس قل لي 😊