بالطبع! إليك **شرح مبسط وأكيد** لقسم **"Device Discovery with CDP"**، بالعربية وبطريقة سهلة تفهمها حتى لو كنت مبتدئ:

---

### 🔍 ما هو CDP؟ (Cisco Discovery Protocol)

- **CDP** هو بروتوكول خاص بشركة **سايسكو فقط**.
- يعمل على **الطبقة الثانية (Layer 2)** من نموذج OSI.
- مهمته: **اكتشاف الأجهزة المجاورة** (مثل الراوترات، السويتشات) المتصلة مباشرةً بك، طالما هي أجهزة سايسكو.
- لا يهم نوع الوسيلة (سلك نحاسي، ألياف ضوئية...) ولا البروتوكول المستخدم (مثل IP، IPv6...) — CDP يعمل بغض النظر عن ذلك ✅.

> 💡 تخيله مثل "بطاقة تعريف" ترسلها كل أجهزة سايسكو تلقائيًا لجيرانها.

---

### 📡 كيف يعمل؟

- كل جهاز سايسكو **يرسل إعلانات (Advertisements)** كل فترة (عادة كل 60 ثانية).
- هذه الإعلانات تحتوي على معلومات مثل:
  - اسم الجهاز (مثلاً: `S1` أو `R1`)
  - نوعه (راوتر؟ سويتش؟ هاتف IP؟)
  - المنافذ المتصلة (مثلاً: منفذ `G0/0/1` في الراوتر متصل بمنفذ `F0/5` في السويتش)
  - إصدار النظام (IOS)، وعنوان IP (إذا كان موجودًا)

---

### ⚙️ تشغيل/إيقاف CDP

#### ✅ افتراضيًا:  
- CDP **مشغل تلقائيًا** على جميع أجهزة سايسكو.

#### الأوامر الأساسية:

| المهمة | الأمر |
|-------|-------|
| 📊 عرض حالة CDP ومعلومات عامة | `show cdp` |
| 📋 عرض الأجهزة المجاورة مباشرةً | `show cdp neighbors` |
| 📄 عرض تفاصيل أكثر (مثل عنوان IP، نوع الجهاز) | `show cdp neighbors detail` |
| 🔌 إيقاف CDP على **منفذ واحد فقط** (مثلاً لأمان) | في وضع interface: `no cdp enable` |
| 🔌 إعادة تشغيله على المنفذ | `cdp enable` |
| 🌐 إيقاف CDP على **الجهاز كله** | في وضع global: `no cdp run` |
| 🌐 إعادة تشغيله على كل المنافذ | `cdp run` |
| 📋 عرض المنافذ التي يعمل عليها CDP | `show cdp interface` |

> 💡 ملاحظة مهمة:  
> `no cdp enable` = يوقف الإرسال والاستقبال من **منفذ واحد** فقط.  
> `no cdp run` = يوقفه **كليًا** على الجهاز.

---

### 🗺 كيف نستخدم CDP لرسم خريطة الشبكة؟

تخيل شبكة فيها:
- راوتر اسمه `R1`
- سويتش اسمه `S1` متصل به عبر منفذ `G0/0/1` في الراوتر ⟷ `F0/5` في السويتش

#### 1. تشغّل الأمر:
```bash
R1# show cdp neighbors
```
تشوف مثلاً:
```
Device ID   Local Intrfce     Holdtme     Capability   Platform     Port ID
S1          Gig 0/0/1         179         S I          WS-C3560     Fas 0/5
```
- معناه: الجهاز `S1` (سويتش) متصل بمنفذ `Gig0/0/1` في `R1`.

#### 2. عايز تعرف عنوان IP بتاع السويتش؟  
تستخدم:
```bash
R1# show cdp neighbors detail
```
في الناتج تلاقي:
```
IP address: 192.168.1.2
```
✅ الآن عندك الخريطة:  
`R1 (G0/0/1) ←→ S1 (F0/5)`  
وعندك IP السويتش: `192.168.1.2`

> ✔️ ده مفيد جدًا لو نسيت اسم سويتش جديد، أو لسه ما خصّصتش له IP!

---

### 🛠 تطبيق عملي (Packet Tracer – المطلوب في الشرح)

المطلوب:  
> "رسم خريطة لفرع بعيد، واكتشاف اسم سويتش جديد **لم يُضبط له IP بعد**."

#### كيف؟
1. تتصل بالراوتر الرئيسي عن طريق **SSH** (لأن IP السويتش مجهول).
2. من الراوتر، تشغل:
   ```bash
   show cdp neighbors
   ```
3. تلاقي السويتش الجديد (مثلاً اسمه `SW-New`) متصل بمنفذ معين.
4. تستخدم:
   ```bash
   show cdp neighbors detail
   ```
   — حتى لو ما عندهش IP، تقدر تعرف:
   - اسمه
   - نوعه (موديل السويتش)
   - منفذ الاتصال
   - وقت البقاء (Holdtime)
5. ترسم الخريطة: من هو متصل بمن؟ ومن أي منفذ؟

---

### ✅ ملخّص سريع (بالنقاط):

| الفكرة | التوضيح |
|--------|---------|
| 🔹 CDP بروتوكول سايسكو فقط | لا يعمل مع أجهزة غير سايسكو (مثل هواوي، جونيبر) |
| 🔹 يعمل على Layer 2 | يعني يشتغل حتى لو ما فيش إعدادات IP بعد! |
| 🔹 مفيد جدًا في | استكشاف الأخطاء، رسم الخرائط، إدارة الشبكة |
| 🔹 يُستخدم كثيرًا في | شبكات الشركات، المختبرات، شهادات مثل CCNA |
| 🔹 أمان؟ | يُنصح بإيقافه على المنافذ الخارجية (مثل منافذ facing Internet) لمنع تسريب معلومات. |

---

إذا حابب، أقدر أرسل لك **رسم تخطيطي بسيط** أو **سيناريو عملي خطوة بخطوة** مثل المطلوب في Packet Tracer 🎯  
بس قل لي!