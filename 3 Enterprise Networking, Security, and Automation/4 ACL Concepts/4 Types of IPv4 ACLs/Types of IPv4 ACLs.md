### شرح بسيط لقسم Types of IPv4 ACLs

مرحبا! هالقسم بيتكلم عن "أنواع قوائم التحكم في الوصول لـ IPv4 (ACLs)" في الشبكات، خاصة في راوترات Cisco. ACLs هي قواعد تسمح أو تمنع مرور الحزم (packets) بناءً على شروط معينة. هشرحها خطوة بخطوة بأبسط طريقة، بدون تعقيدات.

#### 1. **أنواع ACLs الأساسية (Standard and Extended ACLs):**
- **Standard ACLs**: 
  - تركز فقط على عنوان المصدر (source IPv4 address).
  - تسمح أو تمنع الحزم بناءً على من أين جاءت (مثل "امنع كل الحركة من شبكة معينة").
  - بسيطة، لكن محدودة (مش بتشوف الوجهة أو النوع).

- **Extended ACLs**:
  - أقوى وأكثر تفصيلاً.
  - تشوف عنوان المصدر، الوجهة (destination IPv4 address)، نوع البروتوكول (مثل TCP أو UDP)، أرقام المنافذ (ports) للمصدر والوجهة، وأشياء أخرى.
  - مثال: "اسمح بـ FTP من شبكة X إلى Y، لكن امنع Telnet".

#### 2. **طرق تسمية ACLs (Numbered and Named ACLs):**
- **Numbered ACLs** (بالأرقام):
  - تستخدم أرقام لتحديد النوع:
    - Standard: 1-99 أو 1300-1999.
    - Extended: 100-199 أو 2000-2699.
  - مثال في الـ config: `access-list 10 permit 192.168.1.0 0.0.0.255` (هذا standard، رقم 10).
  - سهل، لكن مش واضح الغرض من الرقم.

- **Named ACLs** (بالأسماء):
  - أفضل طريقة، عشان تعطي اسم يوصف الغرض (مثل "FTP-FILTER" بدل رقم 100).
  - كيف تنشئها: استخدم `ip access-list extended FTP-FILTER` (ثم أضف القواعد داخلها).
  - مثال:
    ```
    R1(config)# ip access-list extended FTP-FILTER
    R1(config-ext-nacl)# permit tcp 192.168.10.0 0.0.0.255 any eq ftp
    R1(config-ext-nacl)# permit tcp 192.168.10.0 0.0.0.255 any eq ftp-data
    ```
  - فائدة: أسهل فهم وصيانة، خاصة في شبكات كبيرة.

#### 3. **أين تضع ACLs (Where to Place ACLs):**
- **القاعدة العامة**: ضع ACL حيث يكون أكثر كفاءة وتأثير.
  - ال**Extended ACLs**: قريب من المصدر قدر الإمكان (عشان توقف الحركة الغير مرغوبة قبل ما تسافر بعيد وتستهلك bandwidth).
  - ال**Standard ACLs**: قريب من الوجهة (عشان مش بتشوف الوجهة، لو وضعتها عند المصدر ممكن تمنع حركة مهمة).

- **العوامل اللي تؤثر في المكان:**
  - **سيطرة المنظمة**: ضع ACL على الأجهزة اللي تسيطر عليها (مش على راوترات الآخرين).
  - **Bandwidth**: امنع الحركة الغير مرغوبة عند المصدر عشان توفر عرض النطاق.
  - **سهولة الإعداد**: أحيانًا أسهل عند الوجهة، لكن ممكن يضيع bandwidth.

#### 4. **مثال على وضع Standard ACL:**
- سيناريو: امنع حركة من شبكة 192.168.10.0/24 إلى 192.168.30.0/24.
- أفضل مكان: على راوتر R3 (قريب الوجهة).
  - **خيارات الواجهات على R3:**
    - Inbound على S0/1/1: سيمنع حركة .10 إلى .30، لكن ممكن يمنع إلى شبكات أخرى (مثل .31) – مش جيد.
    - Outbound على G0/0: يمنع فقط إلى .30، بدون تأثير على الآخرين – هذا الأفضل.

#### 5. **مثال على وضع Extended ACL:**
- سيناريو: امنع Telnet و FTP من 192.168.11.0/24 إلى 192.168.30.0/24، لكن اسمح الباقي (بين شركتين A و B).
- أفضل مكان: على راوتر R1 (قريب المصدر، اللي تسيطر عليه شركة A).
  - **خيارات الواجهات على R1:**
    - Outbound على S0/1/0: جيد، لكن يفحص كل الحركة الخارجة (بما فيها من شبكات أخرى).
    - Inbound على G0/0/1: أفضل، عشان يفحص فقط حركة من 192.168.11.0/24، ويوفر معالجة.

#### تلخيص سريع:
ACLs لـ IPv4 تنقسم إلى standard (بسيطة، مصدر فقط) و extended (متقدمة، مصدر+وجهة+منافذ). استخدم named بدل numbered للوضوح. ضعها بحكمة: extended عند المصدر، standard عند الوجهة، مع مراعاة السيطرة والكفاءة. الأمثلة توضح إن الاختيار الخاطئ ممكن يسبب مشاكل زي هدر bandwidth أو منع حركة غير مقصودة.

لو عندك أسئلة أو تبي توضيح لمثال، قولي! 😊