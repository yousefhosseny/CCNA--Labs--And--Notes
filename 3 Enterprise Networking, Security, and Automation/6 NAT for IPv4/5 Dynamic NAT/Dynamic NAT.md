بالطبع! هشرح لك **Dynamic NAT** (الترجمة الديناميكية) **بأبسط طريقة ممكنة** — كأنك بتسمع القصة من صديق تقني، من غير تعقيد، وبأمثلة من الواقع 🌟

> ⚠️ أولًا: في شوية أخطاء في العنوان — المفروض القسم ده عن **Dynamic NAT**، مش "Static NAT".  
> (ربما كان Copy-Paste من سلايدات، لكن المحتوى صح ✅)

---

## 🌟 إيه هو الـ **Dynamic NAT**؟  
هو نوع من الترجمة (NAT) بيخلي **عدة أجهزة داخلية** (مثلاً: 10 أجهزة في شركة) تستخدم **مجموعة عناوين عامة محدودة** (مثلاً: 5 عناوين فقط)، وبطريقة ذكية:  
> ✅ **أول واحد يطلب، ياخُد أول عنوان حر** — زي ما تاخُد دور في البنك أو الكاشير 🏦

---

### 📌 سيناريو واقعي (تخيل معايا):
- عندك 10 أجهزة في مكتب: `192.168.10.10`، `192.168.11.10`، … إلخ.
- عندك **مجموعة (Pool)** من 5 عناوين عامة فقط:  
  من `209.165.200.226` إلى `209.165.200.230`
- لما أي جهاز يطلب يتصفح الإنترنت:
  - الراوتر بيديله أول عنوان متاح من القائمة.
  - لو 5 أجهزة اتصّلت مع بعض → كل واحد أخد عنوان.
  - لو الجهاز السادس جه → لازم ينتظر لحد ما واحد يكمل ويحرّر العنوان!

> ❗ ملاحظة مهمة:  
> Dynamic NAT **مش بيستخدم البورتات** (مثل PAT)، فكل جهاز يحتاج عنوان عام كامل لنفسه — حتى لو بس فتح موقع واحد!  
> → فلو عدد المستخدمين المتزامنين > عدد العناوين، في-users هيمشوا في "انتظار" 🚧

---

## 🔧 إزاي تعدّله في الراوتر؟ (5 خطوات ببساطة)

### ✅ الخطوة 1: عرّف "المجموعة" (Pool) من العناوين العامة
```bash
R2(config)# ip nat pool NAT-POOL1 209.165.200.226 209.165.200.240 netmask 255.255.255.224
```
> هذا معناه:  
> - اسم المجموعة: `NAT-POOL1`  
> - العناوين من `226` إلى `240` (يعني 15 عنوان)  
> - الماسك (`255.255.255.224`) يحدد إن المجموعة كلها في Subnet واحد.

---

### ✅ الخطوة 2: عيّن مين اللي يحق له الترجمة (بـ Access List)
```bash
R2(config)# access-list 1 permit 192.168.0.0 0.0.255.255
```
> هذا معناه:  
> - اسم القائمة: `1`  
> - "اسمح بترجمة أي جهاز عنوانه يبدأ بـ `192.168.x.x`"  
> (الـ `0.0.255.255` اسمه **Wildcard Mask** — بيعني "تجاهل آخر جزأين")

---

### ✅ الخطوة 3: وصّل القائمة بالمجموعة
```bash
R2(config)# ip nat inside source list 1 pool NAT-POOL1
```
> "خلي الأجهزة المسموح ليها في القائمة 1 تستخدم العناوين من المجموعة NAT-POOL1"

---

### ✅ الخطوة 4 و 5: عيّن الواجهات (داخل وخارج)
```bash
R2(config)# interface Serial0/1/0
R2(config-if)# ip nat inside     ← جهة الشبكة الداخلية

R2(config)# interface Serial0/1/1
R2(config-if)# ip nat outside    ← جهة الإنترنت
```

> ✅ تمام! كده التكوين انتهى.

---

## 🔄 شوف إزاي بيشتغل (بالحركة):

### ➡️ من الداخل للخارج (جهازان يفتحوا موقع):
1. **PC1** (`192.168.10.10`) و**PC2** (`192.168.11.10`) بيطلبوا موقع.
2. الراوتر:
   - يستلم طلب PC1 → يشوف القائمة → يختار أول عنوان حر: `209.165.200.226`  
     ويضيف في الجدول:  
     `192.168.10.10` ←→ `209.165.200.226`
   - يستلم طلب PC2 → يختار التاني: `209.165.200.227`  
     الجدول:  
     `192.168.11.10` ←→ `209.165.200.227`
3. بعدها يبعت الطلب للموقع، وكل واحد عنده عنوان مختلف.

---

### ⬅️ من الخارج للداخل (الردود):
1. الموقع يردّ على `209.165.200.226` → الراوتر يشوف الجدول ويقول:  
   "ده لـ `192.168.10.10`" → يحوّله لـ PC1.
2. الموقع يردّ على `209.165.200.227` → الراوتر يحوّله لـ PC2.

> 🔁 كل باكيت بيعدي على الراوتر → بيتم ترجمته باستخدام الجدول.

---

## 🔍 إزاي تتأكد إنه شغال؟ (أوامر التحقق)

### 1. شوف الترجمات النشطة:
```bash
R2# show ip nat translations
```
المخرجات:
```
Inside global       Inside local
209.165.200.226     192.168.10.10
209.165.200.227     192.168.11.10
```

> ⚠️ لو مفيش اتصال حالي — الجدول هيكون فاضي (عكس Static NAT اللي بيظهر دايماً).

---

### 2. شوف تفاصيل أكثر (وقت الإنشاء، المدة المتبقية…):
```bash
R2# show ip nat translations verbose
```
بيظهر:
- `create 00:02:11` ← تم إنشاء الترجمة قبل دقيقتين و11 ثانية  
- `left 23:57:48` ← هيتحذف بعد كده بـ 24 ساعة (إلا لو خلص الاتصال قبل كده)

> 💡 معلومة: الترجمة تتحذف بعد **24 ساعة من عدم الاستخدام** (مفيش حركة)، أو لما الاتصال يخلص.

---

### 3. امسح الترجمات يدويًا (لو عاوز تجربة جديدة):
```bash
R2# clear ip nat translation *
```
> يمسح كل الترجمات الديناميكية — الترجمات الثابتة (Static) مش بتتمسح.

---

### 4. شوف الإحصائيات:
```bash
R2# show ip nat statistics
```
بيظهر لك:
- عدد الترجمات النشطة  
- عدد العناوين في الـ Pool  
- كام عنوان اتستخدم (`allocated 2`)  
- عدد المرات اللي نجحت (`Hits: 47`) أو فشلت (`Misses: 0`)  
- عدد العناوين اللي انقضى وقتها (`Expired translations: 5`)

> مثال من الإخراج:
```
pool NAT-POOL1: total addresses 15, allocated 2 (13%)
```
يعني: 15 عنوان متوفرة، واتستخدموا 2 بس — مفيش ضغط.

---

### 5. شوف الإعدادات الحالية بسرعة:
```bash
R2# show running-config | include NAT
```
بيوري لك كل السطور اللي فيها كلمة "NAT" — سهل جدًا للمراجعة.

---

## 🆚 Dynamic NAT vs Static NAT vs PAT (مقارنة سريعة)

| النوع | عدد العناوين المطلوبة | هل ينتظر المستخدم؟ | هل بيستخدم البورت؟ | الاستخدام |
|------|------------------------|---------------------|---------------------|-----------|
| **Static NAT** | 1 عنوان لكل جهاز | ❌ لا | ❌ لا | سيرفرات (موقع، FTP…) |
| **Dynamic NAT** | عنوان لكل اتصال **متزامن** | ✅ نعم (لو انتهى الـ Pool) | ❌ لا | شبكات صغيرة/متوسطة |
| **PAT (NAT Overload)** | عنوان واحد كفاية | ❌ لا (حتى مع آلاف المستخدمين) | ✅ نعم | ✅ **كل البيوت والشركات الكبيرة** |

---

## ✅ خلاصة "بالعربي الفصيح البسيط":
> Dynamic NAT = "عنوان عام مؤقت من قائمة انتظار"  
> - كل جهاز يدخل، ياخُد أول عنوان متوفر.  
> - لما يخرج أو ينتهي الاتصال، العنوان يرجع للقائمة.  
> - لو القائمة خلصت، اللي جاي يقف في طابور 🧍‍♂️🧍‍♀️  
> - **مفيش بورتات** → فمش قادر يخدم عدد كبير من المستخدمين بعنوان واحد.

---

لو عاوز أشرح لك الفرق بين Dynamic NAT و PAT بطريقة مصورة، أو أعمل لك مثال عملي في Packet Tracer خطوة بخطوة — قول لي، وهظبطهولك! 🛠️😊