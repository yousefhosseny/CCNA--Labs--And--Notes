بالطبع! إليك شرح **Static NAT** (الترجمة الثابتة) **بأبسط طريقة ممكنة** — كأنك بتشرح لصديق مش مهندس شبكات، وبأمثلة واقعية وبسيطة 😊

---

### 🌟 أولًا: إيه هو الـ **Static NAT**؟  
هو نوع من الترجمة (NAT) بيخلّي **جهاز داخلي** (مثل سيرفر ويب في الشركة) **يكون متاح من الإنترنت** بعنوان عام ثابت.

> ⚠️ ملحوظة مهمة:  
> معظم أجهزة الشبكة الداخلية (مثل اللاب توب أو الموبايل) تستخدم **عناوين خاصة** (مثل `192.168.x.x`) — وده العنوان **مش شغال على الإنترنت**.  
> فـ Static NAT بيقول:  
> ✅ "خلي الجهاز ده (مثلاً: سيرفر ويب) يظهر على الإنترنت **بدون تغيير**، ويعنيه دايماً نفس العنوان العام."

---

### 🖥️ مثال واقعي جدًا:
- عندك **سيرفر ويب داخلي** في الشركة، عنده العنوان:  
  `192.168.10.254` ← ده عنوان خاص، مبيظهرش على الإنترنت.
- عاوز تخلّيه يظهر كموقع إلكتروني للزبائن، فـ بتحبّه بعنوان عام دائم مثل:  
  `209.165.201.5`

→ هنا تستخدم **Static NAT**، وتقول للراوتر:  
> "**كل طلب ييجيلي موجّه لعنوان `209.165.201.5`، خدّمه على الجهاز `192.168.10.254` داخليًا.**"

وكل طلب يبعته السيرفر داخليًا، تغيّر مصدره من `192.168.10.254` إلى `209.165.201.5` قبل ما يروح للإنترنت.

---

### 🔧 كيف تُعدّله عمليًا؟ (خطوتين فقط)

#### ✅ الخطوة 1: سجّل الترجمة (الخريطة الثابتة)
في راوتر سيسكو، تكتب الأمر ده:

```bash
R2(config)# ip nat inside source static 192.168.10.254 209.165.201.5
```

> هذا معناه:  
> - الجهاز داخليًا: `192.168.10.254`  
> - هيظهر للخارج: `209.165.201.5`  
> - العلاقة **ثابتة وواحد لواحد** — مش بتتغير أبدًا.

#### ✅ الخطوة 2: عيّن الواجهات (Interfaces)
لازم تحدد:
- فين داخلي (inside) ← الشبكة الداخلية (حيث السيرفر)
- وفين خارجي (outside) ← جهة الإنترنت

مثال:

```bash
R2(config)# interface Serial0/1/0
R2(config-if)# ip nat inside     ← هذه الواجهة داخل الشبكة

R2(config)# interface Serial0/1/1
R2(config-if)# ip nat outside    ← هذه الواجهة متصلة بالإنترنت
```

> ✅ تمام! كده السيرفر صار "ظاهر" على الإنترنت.

---

### 🔄 إزاي بيشتغل عمليًا؟ (الحركة بتاعة الباقات)

تخيل إن مستخدم من الإنترنت (`209.165.200.254`) عاوز يفتح موقعك:

1. المستخدم يرسل طلب لـ `209.165.201.5`  
2. الراوتر يستلم الطلب → يشوف في جدول الـ NAT:  
   > "أه، `209.165.201.5` معناها `192.168.10.254` داخليًا"  
   → فيُغيّر وجهة الباكيت لـ `192.168.10.254` ويخلّيه يوصل للسيرفر.  
3. السيرفر يردّ على الطلب، ويبعت الرد من عنوانه (`192.168.10.254`)  
4. الراوتر يستلم الرد → بيقول:  
   > "لازم أغيّر المصدر من `192.168.10.254` لـ `209.165.201.5` قبل ما أرسله للخارج"  
   → ويخلّيه يوصل للمستخدم بسلاسة.

> 💡 الترجمة بتتم في **اتنين اتجاه**:  
> - الطلب: خارجي → داخلي (يُترجم **الوجهة**)  
> - الرد: داخلي → خارجي (يُترجم **المصدر**)

---

### 🔍 إزاي تتأكد إنه شغال؟ (أوامر التحقق)

#### 1. شوف جدول الترجمة:
```bash
R2# show ip nat translations
```
المخرجات بتكون كده:
```
Inside global       Inside local
209.165.201.5       192.168.10.254
```
> ✅ طالما شايف السطر ده، يبقى الترجمة مسجّلة — حتى لو مفيش اتصال حالي.

إذا كان في اتصال نشط، تظهر معلومات إضافية مثل:
```
Outside local       Outside global
209.165.200.254     209.165.200.254
```

#### 2. شوف الإحصائيات:
```bash
R2# show ip nat statistics
```
بيظهرلك:
- عدد الترجمات النشطة  
- عدد المرات اللي استخدمت فيها الترجمة (`Hits`)  
- عدد المرات اللي مالقتش ترجمة (`Misses`) ← لو كتير، في مشكلة!

> 💡 نصيحة: قبل الاختبار، نظّف الإحصائيات بـ:
```bash
R2# clear ip nat statistics
```
عشان تشوف الأرقام الجديدة فقط.

---

### 🧪 تجربة عملية (في Packet Tracer):
التمرين اللي في الشرائح بيطلب منك:
1. **جرب تفتح الموقع من غير NAT** → هتلاقيه مش شغال (لأن العنوان خاص).
2. **شغّل Static NAT** → تطبيق الخطوتين اللي فوق.
3. **جرب تاني** → الموقع اشتغل! 🎉

---

### ✅ ملخّص بسيط جدًا (مثل المُلخّصات اللي في التوك شو):

| الفكرة | التوضيح |
|--------|---------|
| **Static NAT = سيرفر على الإنترنت** | زي ما تسمّي محلك برقم ثابت في الدليل التليفوني 📞 |
| **واحد لواحد** | كل عنوان داخلي ← عنوان خارجي واحد ومحدّد |
| **لازم عنوان عام لكل سيرفر** | لو عندك 3 سيرفرات، تحتاج 3 عناوين عامة |
| **مبيتغيرش** | الترجمة دايماً موجودة في الجدول — حتى لو مفيش اتصال |

---

لو عاوز أمثلة على إزاي تستخدم Static NAT لسيرفر FTP أو كاميرات مراقبة — أو حتى شرح فيديو بسيط بالرسم — أنا جاهز! 🎯