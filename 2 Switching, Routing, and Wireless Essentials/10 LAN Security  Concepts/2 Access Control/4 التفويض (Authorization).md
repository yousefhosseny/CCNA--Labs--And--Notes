### 4. **التفويض (Authorization)**
- بعد المصادقة، AAA بيحدد إيه اللي المستخدم يقدر يعمله (مثل الوصول لأوامر معينة أو موارد معينة).
- التفويض بيحصل تلقائيًا بناءً على إعدادات السيرفر، وما بيطلبش خطوات إضافية من المستخدم.
- السيرفر بيستخدم قائمة بالصلاحيات (Attributes) عشان يحدد حدود وصول المستخدم.
