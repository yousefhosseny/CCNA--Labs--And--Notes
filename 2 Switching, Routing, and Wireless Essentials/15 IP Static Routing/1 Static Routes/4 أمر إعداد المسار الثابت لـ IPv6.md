
### 4. **أمر إعداد المسار الثابت لـ IPv6**
- الأمر:
  ```bash
  ipv6 route ipv6-prefix/prefix-length {ipv6-address | exit-intf [ipv6-address]} [distance]
  ```
- نفس فكرة IPv4، بس بتستخدم عناوين IPv6 وطول الـ Prefix (مثل /64).

---
