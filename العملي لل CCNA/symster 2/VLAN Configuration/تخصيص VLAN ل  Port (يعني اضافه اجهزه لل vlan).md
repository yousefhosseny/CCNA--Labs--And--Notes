
### 📌 الفكرة

إنت بعد ما تعمل VLAN (مثلاً VLAN 20)، لازم تربطها بمنفذ معين في السويتش عشان أي جهاز يتوصل على المنفذ ده يشتغل ضمن نفس الـ VLAN.

---

### 🛠 الخطوات العملية

1. **ادخل وضع الإعدادات العامة (Global Configuration):**
    
    ```
    Switch# configure terminal
    ```
    
2. **اختيار المنفذ اللي عايز تربطه (مثلاً fa0/18):**
    
    ```
    Switch(config)# interface fa0/18
    ```
    
3. **خلي المنفذ Access Port** (يعني هيكون تابع لـ VLAN واحدة بس، مش Trunk):
    
    ```
    Switch(config-if)# switchport mode access
    ```
    
4. **اربط المنفذ بالـ VLAN المطلوبة (مثلاً VLAN 20):**
    
    ```
    Switch(config-if)# switchport access vlan 20
    ```
    
5. **ارجع للوضع التنفيذي (Privileged EXEC):**
    
    ```
    Switch(config-if)# end
    ```
    

---

### ✅ مثال كامل

عايز أخلي المنفذ **fa0/18** ينتمي لـ VLAN 20:

```
 configure terminal
interface fa0/6
switchport mode access
switchport access vlan 30
 end
```

---

### 📌 النتيجة

- أي جهاز (كمبيوتر، برنتر، …) يتوصل بالمنفذ **fa0/18** هيبقى عضو في VLAN 20.
    
- الجهاز لازم ياخد IP من نفس شبكة الـ VLAN دي (مثلاً 172.17.20.x).
    

---

تحب أشرحلك كمان إزاي تتحقق إن المنفذ بقى مربوط بالـ VLAN (بالأوامر `show vlan brief` أو `show running-config`)؟

