
### **شرح إنشاء VLAN على السويتش**

🔹 **الفكرة الأساسية**  
الـ VLAN (Virtual LAN) بتقسم الشبكة جوه السويتش لعدة شبكات منطقية. كل VLAN تعتبر broadcast domain منفصل.  
البيانات الخاصة بالـ VLANs بتتخزن في ملف اسمه **vlan.dat** على الفلاش بتاع السويتش.

---

### **الخطوات العملية:**

1. **ادخل على وضع الإعدادات العامة (Global Configuration Mode):**
    
    ```
    Switch# configure terminal
    ```
    
    - هنا بتبدأ تدخل أوامر التهيئة.
        
2. **أنشئ VLAN برقم محدد** (مثلاً: VLAN 20):
    
    ```
    Switch(config)# vlan 20
    ```
    
    - كده السويتش أنشأ VLAN برقم 20.
        
    - لو الرقم مش موجود قبل كده، السويتش هيضيفه.
        
3. **تسمية الـ VLAN (اختياري):**
    
    ```
    Switch(config-vlan)# name student
    ```
    
    - لو ما كتبتش اسم، هيظهر بالاسم الافتراضي زي `VLAN0020`.
        
    - الاسم بيسهل عليك تعرف الغرض من VLAN دي (مثلاً: student, admin, HR).
        
4. **الخروج من وضع الإعدادات:**
    
    ```
    Switch(config-vlan)# end
    ```
    
    أو تقدر تستخدم:
    
    ```
    Switch(config-vlan)# exit
    Switch(config)# exit
    ```
    

---

### **مثال كامل عملي:**

```
 configure terminal
 vlan 20
 name student
 end
```


---

### **التحقق من إنشاء الـ VLAN:**

بعد ما تخلص، تأكد إن VLAN اتعملت:

```
S1# show vlan brief
```

هتلاقي VLAN 20 موجودة بالاسم `student`.

---

تحب أشرحلك كمان **إزاي نربط بورت معين بالـ VLAN دي** (عشان الأجهزة فعلاً تستخدمها)؟