
### 🎯 الفكرة الأولية:

- أي **Access Port** عادي ينفع يتربط بـ VLAN واحد للداتا فقط.
    
- لكن لما يكون عندك **هاتف VoIP + كمبيوتر** متوصلين بنفس البورت (الكمبيوتر واصل من خلال الهاتف)، محتاج:
    
    - VLAN خاصة بالـ **Data** (الكمبيوتر).
        
    - VLAN خاصة بالـ **Voice** (الهاتف).
        

وده بيسمح للسويتش يفصل الترافيك ويضمن إن **الصوت له أولوية** (QoS).

---

### 🛠️ الخطوات العملية:

1. **إنشاء VLANات:**
    
    - VLAN 20 → للداتا (مثلاً لأجهزة الطلبة).
        
    - VLAN 100 → للصوت (VoIP).
        
    
    ```bash
    Switch# configure terminal
    Switch(config)# vlan 20
    Switch(config-vlan)# name student
    Switch(config)# vlan 100
    Switch(config-vlan)# name voice
    ```
    
2. **توصيف المنفذ (fa0/18 كمثال):**
    
    - تخليه Access Port.
        
    - تعيين VLAN 20 كـ Data VLAN.
        
    - تعيين VLAN 100 كـ Voice VLAN.
        
    - تفعيل QoS عشان الترافيك بتاع الصوت ياخد أولوية.
        
    
    ```bash
    Switch(config)# interface fa0/18
    Switch(config-if)# switchport mode access
    Switch(config-if)# switchport access vlan 20
    Switch(config-if)# switchport voice vlan 100
    Switch(config-if)# mls qos trust cos
    Switch(config-if)# end
    ```
    



---

### ✅ اللي حصل هنا:

- الكمبيوتر المتوصل على نفس البورت → هيشتغل على VLAN 20 (Data).
    
- التليفون VoIP → هيشتغل على VLAN 100 (Voice).
    
- QoS (Quality of Service) مفعلة → الصوت يتنقل بأولوية أعلى، فالمكالمات مش هتقطع أو يحصل فيها Delay.
    

---

تحب أشرحلك **إزاي الكمبيوتر والهاتف بيبقوا متوصلين في نفس البورت وازاي السويتش بيفرق الترافيك بينهم**؟