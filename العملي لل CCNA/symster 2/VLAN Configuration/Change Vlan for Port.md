تمام ✅ خليني أشرحلك الموضوع كأن عندك المنفذ **FastEthernet 0/1** متعيَّن على **VLAN 30**، وعايز تفهم الخطوات لتغييره أو إرجاعه:

---

### 🔹 الحالة الأولية:

المنفذ **Fa0/1** مربوط دلوقتي بـ VLAN 30.  
وده تم عن طريق الأمر:

```
Switch(config-if)# switchport access vlan 30
```

---

### 🔹 لو عايز تغيّر VLAN المنفذ:

1. ادخل على إعدادات الواجهة:
    
    ```
    Switch(config)# interface fa0/1
    ```
    
2. خصص VLAN جديدة (وليكن VLAN 20):
    
    ```
    Switch(config-if)# switchport access vlan 20
    ```
    
    👉 كده المنفذ بدل ما كان في VLAN 30، بقى في VLAN 20.
    

---

### 🔹 لو عايز ترجّع المنفذ للـ VLAN الافتراضية (VLAN 1):

1. ادخل على نفس الواجهة:
    
    ```
    Switch(config)# interface fa0/1
    ```
    
2. امسح التخصيص اليدوي:
    
    ```
    Switch(config-if)# no switchport access vlan
    ```
    
    👉 كده بيرجع أوتوماتيك لـ VLAN 1 (الافتراضية).
    

---

### 🔹 التحقق بعد التغيير:

- لعرض كل الـ VLANs والمنافذ اللي جواها:
    
    ```
    Switch# show vlan brief
    ```
    
    → هتلاقي Fa0/1 ظهر في VLAN الجديدة أو في VLAN 1 لو رجعته.
    
- لعرض إعدادات المنفذ نفسه:
    
    ```
    Switch# show interfaces fa0/1 switchport
    ```
    
    → هيبينلك الـ VLAN المربوط بيها المنفذ.
    

---

📝 **باختصار لمثال Fa0/1:**

- كان في VLAN 30.
    
- ممكن تغيّره لأي VLAN تانية (زي 20).
    
- أو ترجّعه للـ VLAN 1 بالأمر `no switchport access vlan`.
    
- وتتأكد من النتيجة بالأوامر `show vlan brief` أو `show interfaces fa0/1 switchport`.
    

---

تحب أعملك مقارنة بالأوامر قبل وبعد (يعني أوت بوت `show vlan brief` في حالة Fa0/1 وهو في VLAN 30، وبعد ما نرجعه VLAN 1)؟