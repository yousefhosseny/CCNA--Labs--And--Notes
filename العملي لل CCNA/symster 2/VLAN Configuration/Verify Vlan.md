
### 🔎 **فحص إعدادات الـ VLAN**

#### 1. **عرض كل الـ VLANs**

استخدم:

```bash
Switch# show vlan brief
```

- ده بيعرض لك **كل الـ VLANs اللي متعرفة** على السويتش.
    
- هتشوف:
    
    - رقم الـ VLAN (ID).
        
    - اسم الـ VLAN.
        
    - حالتها (active ولا لأ).
        
    - المنافذ المرتبطة بيها (Ports).
        

---

#### 2. **عرض VLAN معينة بالـ ID**

مثال:

```bash
Switch# show vlan id 20
```

- ده بيجيبلك تفاصيل عن **VLAN رقم 20** بس.
    
- بتشوف نفس المعلومات بس بشكل محدد للـ VLAN دي.
    

---

#### 3. **عرض VLAN معينة بالاسم**

مثال:

```bash
Switch# show vlan name student
```

- لو عندك VLAN اسمها **student**، هتشوف تفاصيلها.
    
- مفيدة لو مش فاكر الـ ID بس عارف الاسم.
    

---

#### 4. **ملخص عن كل الـ VLANs**

```bash
Switch# show vlan summary
```

- ده بيطلع **ملخص سريع** عن عدد الـ VLANs المتعرفة، وعددهم في الحالة active أو suspended.
    

---

#### 5. **عرض إعدادات منفذ محدد (switchport)**

مثال:

```bash
Switch# show interfaces fa0/18 switchport
```

- ده بيوضحلك **إعدادات المنفذ fa0/18**، زي:
    
    - هو Access port ولا Trunk.
        
    - مربوط بأي VLAN (لو Access).
        
    - لو Trunk، أي VLANs مسموح تمر فيه.
        
    - إعدادات الـ Voice VLAN (لو مفعلة).
        

---

📌 **الخلاصة:**

- `show vlan brief` → نظرة عامة على كل الـ VLANs.
    
- `show vlan id X` أو `show vlan name Y` → تفاصيل VLAN معينة.
    
- `show vlan summary` → ملخص سريع.
    
- `show interfaces switchport` → إعدادات منفذ معين.
    

تحب أعملك مثال عملي بسيناريو VLAN 10 و 20 مع منفذ fa0/18 عشان يوضح أكتر؟