
### 🔹 **حذف VLAN معيّن**

- لو عندك VLAN (مثلاً رقم 20) وعايز تمسحه:
    
    ```
    Switch(config)# no vlan 20
    ```
    
- هنا السويتش هيشيل تعريف الـ VLAN ده من جدول الـ VLANs.
    
- **مهم جدًا**: أي منفذ كان متعيّن على VLAN 20 لازم ترجّعه الأول على VLAN تانية (زي VLAN 1 الافتراضية) عشان المنفذ مايبقاش معزول.
    

---

### 🔹 **حذف كل الـ VLANs مرّة واحدة**

- كل بيانات الـ VLANs (زي أرقامهم وأسمائهم) بتتخزن في ملف اسمه **vlan.dat** موجود في الفلاش (Flash Memory) بتاع السويتش.
    
- عشان تمسحهم كلهم:
    
    ```
    Switch# delete flash:vlan.dat
    ```
    
- بعد كده تعمل **reload** (إعادة تشغيل) عشان السويتش يشتغل من غير الملف ده.
    

---

### 🔹 **إرجاع السويتش للإعدادات الافتراضية (Factory Reset)**

لو عايز السويتش يرجع كأنه جديد (من المصنع):

1. افصل كل كابلات البيانات (عشان مايحاولش ياخد إعدادات أو DHCP من شبكة تانية).
    
2. امسح إعدادات الإقلاع (startup-config) من الـ NVRAM:
    
    ```
    Switch# erase startup-config
    ```
    
3. امسح ملف الـ VLANs (vlan.dat):
    
    ```
    Switch# delete flash:vlan.dat
    ```
    
4. اعمل ريستارت للجهاز:
    
    ```
    Switch# reload
    ```
    

- بعد الخطوات دي السويتش هيشتغل من الأول كأنه لسه خارج من الكرتونة:
    
    - مفيش VLANs مخصصة.
        
    - مفيش إعدادات محفوظة.
        
    - كل المنافذ في VLAN 1 الافتراضية.
        

---

تحب أجهزلك مثال عملي (سيناريو صغير) فيه VLAN 20 معمول، وبعد كده نمسحه خطوة بخطوة ونرجّع السويتش Default؟