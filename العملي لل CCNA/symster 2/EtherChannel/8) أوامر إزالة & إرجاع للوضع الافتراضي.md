
- لإزالة العضوية من مجموعة:
    
    ```
    S1(config)# interface range fa0/1 - 2
    S1(config-if-range)# no channel-group 1
    ```
    
- لإعادة الواجهات للوضع الافتراضي:
    
    ```
    S1(config)# default interface fa0/1
    S1(config)# default interface fa0/2
    ```
    
