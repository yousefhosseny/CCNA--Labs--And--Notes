
### خطوات تفعيل واستخدام **Auto-MDIX** في محولات Cisco:

1. **الدخول على وضع الإعدادات للإنترفيس**
    
    - حدد البورت اللي هتشتغل عليه:
        
    
    ```bash
    S1(config)# interface fastEthernet 0/1
    ```
    
2. **تأكد إن السرعة (speed) و الـ Duplex على وضع Auto**
    
    - ده شرط أساسي علشان Auto-MDIX يشتغل:
        
    
    ```bash
    S1(config-if)# speed auto
    S1(config-if)# duplex auto
    ```
    
3. **تفعيل Auto-MDIX**
    
    - استخدم الأمر:
        
    
    ```bash
    S1(config-if)# mdix auto
    ```
    
4. **الخروج من الإعدادات**
    
    ```bash
    S1(config-if)# exit
    ```
    
5. **التحقق من تفعيل Auto-MDIX**
    
    - تقدر تتأكد باستخدام الأمر:
        
    
    ```bash
    S1# show controllers ethernet-controller fastEthernet 0/1 phy | include Auto-MDIX
    ```
    

---

📌 **ملحوظة مهمة**:

- في سويتشات Cisco الحديثة (زي Catalyst 2960 و 3560) الـ Auto-MDIX بيكون شغال **افتراضيًا**.
    
- لو شغال على أجهزة أحدث، ممكن ماتحتاجش تفعّله يدوي.
    

