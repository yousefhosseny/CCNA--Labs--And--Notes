
## 🧩 3. **تحديد وتعلم MAC Addresses (Limit and Learn MAC Addresses)**

### 💡 الفكرة:

أنت بتحدد السويتش يستقبل كام جهاز على المنفذ ده (افتراضي = 1).  
ولو حد تاني حاول يدخل، السويتش يتصرف حسب وضع الانتهاك (هنشرحه لاحقًا).

### 🛠️ الأوامر:

- **تحديد الحد الأقصى**:
    
    ```bash
    Switch(config-if)# switchport port-security maximum 2
    ```
    
- **أنواع تعلم العناوين:**
    
    1. **Manually (يدوي):**
        
        ```bash
        Switch(config-if)# switchport port-security mac-address 0011.2233.4455
        ```
        
    2. **Dynamically (تلقائي):**  
        السويتش بيتعلم MAC من أول جهاز يتوصل، بس بيتنسى بعد restart.
        
    3. **Sticky (تلقائي مع حفظ):**
        
        ```bash
        Switch(config-if)# switchport port-security mac-address sticky
        ```
        
        → هيضيف الـMAC في الـrunning-config تلقائيًا.  
        لو عملت `copy run start`، بيتحفظ دائمًا.
        

🔍 **للتحقق:**

```bash
Switch# show port-security address
```

بيظهر كل الـMACs اللي السويتش شايفها وآمنة.

---
