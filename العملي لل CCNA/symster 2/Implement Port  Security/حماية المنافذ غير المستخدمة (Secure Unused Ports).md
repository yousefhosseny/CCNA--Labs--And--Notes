
## 🧱 1. **حماية المنافذ غير المستخدمة (Secure Unused Ports)**

### 💡 الفكرة:

في أي سويتش، في منافذ كتير مش مستخدمة. لو حد دخل كابل فيها من غير إذنك، ممكن يوصّل لطبقة الشبكة (Layer 2) ويبدأ يعمل هجمات زي:

- **MAC Flooding**
    
- **DHCP Snooping bypass**
    
- **Man-in-the-middle attacks**
    

فلازم نقفل المنافذ دي.

### 🛠️ الخطوات:

1. تدخل على الـconfig mode:
    
    ```bash
    Switch> enable
    Switch# configure terminal
    ```
    
2. تستخدم أمر **range** عشان تقفل مجموعة منافذ مع بعض:
    
    ```bash
    Switch(config)# interface range fastEthernet 0/10 - 24
    Switch(config-if-range)# shutdown
    ```
    
3. لو احتجت تفتح منفذ بعدين:
    
    ```bash
    Switch(config-if)# no shutdown
    ```
    

✅ **الفائدة**: تمنع أي شخص من توصيل جهاز عشوائي بالسويتش.

---
