
## 🚨 5. **أوضاع الانتهاك (Violation Modes)**

### 💡 الفكرة:

لو جهاز غير مصرح ليه حاول يتصل، السويتش بيعمل "انتهاك" ويتصرف حسب الوضع اللي انت مختاره.

|الوضع|التصرف|الملاحظات|
|---|---|---|
|**Shutdown (افتراضي)**|يقفل الـport ويدخله حالة error-disabled|لازم ترجع manually بـ`shutdown` + `no shutdown`|
|**Restrict**|يرمي الـpackets المخالفة، يسجل Syslog ويزود عداد|المنفذ بيكمل شغال|
|**Protect**|يرمي الـpackets فقط بدون تسجيل ولا عداد|أقل حماية|

### 🛠️ الأمر:

```bash
Switch(config-if)# switchport port-security violation restrict
```

### 🔍 التحقق:

```bash
Switch# show port-security interface fastEthernet 0/1
```

بيظهرلك وضع الانتهاك وعدد المخالفات المسجلة.

---
