تمام 👌 خليني أبسطهالك خطوة بخطوة بالعامية المصرية:

### **خطوات أمر الـ Boot System:**

1. **المبدأ**:  
    أي سويتش/راوتر من Cisco لما بيجي يعمل Boot (تشغيل)، بيشوف متغير اسمه **BOOT** → ده اللي بيحدد ملف الـ IOS اللي يشغّله.
    
2. **لو المتغير مش متحدد**:  
    الجهاز بيبدأ يدوّر في الفلاش (flash:) على أول ملف IOS يلاقيه ويشغّله.
    
3. **تحديد ملف IOS معين يدويًا**:  
    أنت ممكن تحدد للجهاز ملف IOS بعينه باستخدام الأمر:
    
    ```
    boot system flash:/<path>/<IOS-filename>
    ```
    
    🔹 مثلًا:
    
    ```
    boot system flash:/c2960-lanbasek9-mz.150-2.SE/c2960-lanbasek9-mz.150-2.SE.bin
    ```
    
    - هنا:
        
        - `flash:/` → يعني مكان الفلاش.
            
        - `c2960-lanbasek9-mz.150-2.SE` → اسم الفولدر اللي فيه نسخة IOS.
            
        - `c2960-lanbasek9-mz.150-2.SE.bin` → اسم ملف IOS نفسه.
            
4. **بعد ما تكتب الأمر**:  
    لازم تعمل **حفظ للإعدادات** عشان ميتلغاش بعد الريستارت:
    
    ```
    write memory
    ```
    
    أو
    
    ```
    copy running-config startup-config
    ```
    
5. **تشوف إعدادات البوت**:  
    عشان تتأكد إن الـ Boot System اتسجل صح:
    
    ```
    show boot
    ```
    

---

🔑 **الخلاصة**:

- السويتش عنده أكتر من نسخة IOS ممكن تكون على الفلاش.
    
- بأمر **boot system** بتحدد نسخة معينة يشتغل بيها.
    
- وبتعمل **save** عشان يحفظ الإعداد ده.
    

تحب أعملك سيناريو عملي كأنه على سويتش (بالأوامر خطوة بخطوة)؟