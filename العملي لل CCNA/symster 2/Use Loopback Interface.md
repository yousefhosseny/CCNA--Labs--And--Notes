
### **3. واجهات Loopback**

#### 1. **إيه هي Loopback Interface؟**

- واجهة **منطقية (Logical Interface)** مش بورت حقيقي.
    
- موجودة جوه الراوتر (أو السويتش اللي بيدعم Layer 3).
    
- **مبتفصلش أبدًا** طول ما الراوتر شغال → يعني دايمًا "up".
    

---

#### 2. **استخداماتها:**

1. **اختبار التوجيه (Routing):**
    
    - لو بتجرب بروتوكولات زي OSPF, EIGRP, BGP → بتستخدم IP بتاع الـ Loopback كـ **Router ID** أو كنقطة ثابتة.
        
2. **محاكاة شبكات إضافية في المعمل (Labs):**
    
    - ممكن تعمل كذا Loopback بواجهات مختلفة كأنك ضايف شبكات جديدة من غير ما تحتاج بورتات فيزيائية.
        
3. **عنوان ثابت دايمًا شغال:**
    
    - لو استخدمته كـ management IP → هتفضل واصل بالراوتر حتى لو بعض البورتات وقعت.
        
4. **محاكاة الإنترنت في اللابات:**
    
    - ساعات في التدريب بيتاخد IP بتاع Loopback كأنه IP عام للاتصال بالإنترنت.
        

---

#### 3. **خطوات إعداد Loopback Interface:**

1. تدخل وضع الإعدادات:
    
    ```bash
    R1(config)# interface loopback 0
    ```
    
    - الرقم `0` هو رقم الواجهة (ممكن تعمل Loopback1, Loopback2 …).
        
2. تديها IP Address:
    
    ```bash
    R1(config-if)# ip address 10.0.0.1 255.255.255.0
    ```
    
3. تخرج من الإعداد:
    
    ```bash
    R1(config-if)# exit
    ```
    

---

#### 4. **ملاحظات مهمة:**

- كل واجهة Loopback لازم تاخد IP مختلف (Unique).
    
- مفيش أمر `no shutdown` → لأنها Up بشكل افتراضي.
    
- ممكن تعمل أكتر من واجهة Loopback وتعتبر كل واحدة شبكة مستقلة.
    

---

🔑 **الخلاصة:**

- Loopback = واجهة منطقية، دايمًا شغالة، بتسهل الاختبارات والتوجيه.
    
- بتتضبط زي أي بورت عادي لكن من غير قلق إنه يفصل.
    

---
